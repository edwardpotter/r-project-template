# =============================================================================
# Environment Variables — Copy to .env and fill in your values
# cp .env.example .env
# =============================================================================

# Project name (used for Docker container naming)
COMPOSE_PROJECT_NAME=my-project-name

# Shiny port (default 3838)
SHINY_PORT=3838

# PostgreSQL (uncomment when using the postgres service)
# POSTGRES_DB=r_project_db
# POSTGRES_USER=r_user
# POSTGRES_PASSWORD=changeme
# POSTGRES_PORT=5432
